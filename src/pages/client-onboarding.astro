---
const pageTitle = "Client Onboarding Interview";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <style>
      :root {
        color-scheme: light;
        font-family: "Space Grotesk", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        line-height: 1.6;
        color: #111;
        background-color: #fefbf1;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        background: #fefbf1;
      }

      a {
        color: inherit;
      }

      main.wrapper {
        padding: 3.5rem 1.5rem 4rem;
      }

      .intro {
        margin: 0 auto;
        max-width: 60rem;
      }

      .intro__title {
        margin: 0;
        font-size: clamp(2rem, 3vw + 1rem, 2.5rem);
        font-weight: 600;
        letter-spacing: -0.01em;
      }

      .intro__lead {
        margin: 0.75rem 0 0;
        font-size: 1rem;
        color: #373737;
      }

      .intro__list {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin: 1.75rem 0 0;
        padding: 0;
        list-style: none;
        font-size: 0.95rem;
        color: #424242;
      }

      .intro__list li {
        display: grid;
        grid-template-columns: 1.75rem auto;
        gap: 0.5rem;
        align-items: baseline;
      }

      .intro__list-number {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 1.75rem;
        height: 1.75rem;
        border: 2px solid #0ea5e9;
        border-radius: 999px;
        font-size: 0.85rem;
        font-weight: 600;
        color: #0b7ac0;
      }

      form.intake-form {
        margin: 2.75rem auto 0;
        padding: clamp(1.75rem, 3vw + 1rem, 2.5rem) clamp(1.5rem, 3vw + 1rem, 2.5rem);
        max-width: 62rem;
        background: #ffffff;
        border: 2px solid rgba(17, 17, 17, 0.12);
        border-radius: 1.5rem;
        box-shadow: 0 18px 48px rgba(15, 23, 42, 0.08);
        display: flex;
        flex-direction: column;
        gap: 3rem;
      }

      fieldset.form-section {
        margin: 0;
        padding: 0;
        border: none;
        display: flex;
        flex-direction: column;
        gap: 1.75rem;
      }

      .form-section__legend {
        margin: 0;
        font-size: 1.2rem;
        font-weight: 600;
        color: #0d3b43;
        letter-spacing: -0.01em;
      }

      .form-grid {
        display: grid;
        gap: 1.5rem;
      }

      .form-grid--two {
        width: 100%;
      }

      @media (min-width: 768px) {
        .form-grid--two {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }
      }

      label.form-field {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
        font-size: 0.95rem;
      }

      .form-field__label {
        font-weight: 600;
        letter-spacing: -0.005em;
      }

      .form-field__hint {
        margin: -0.25rem 0 0;
        font-size: 0.85rem;
        color: #555;
      }

      .form-control {
        appearance: none;
        width: 100%;
        border: 1px solid rgba(17, 17, 17, 0.25);
        border-radius: 0.85rem;
        padding: 0.75rem 0.85rem;
        font-size: 0.95rem;
        background-color: #fff;
        transition: border-color 0.2s ease, box-shadow 0.2s ease;
      }

      .form-control:focus {
        outline: none;
        border-color: #0ea5e9;
        box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.2);
      }

      textarea.form-control {
        resize: vertical;
        min-height: 140px;
        line-height: 1.5;
      }

      textarea.form-control[data-rows="short"] {
        min-height: 110px;
      }

      .form-hint {
        margin: -0.35rem 0 0;
        font-size: 0.8rem;
        color: #6b7280;
      }

      .asset-input {
        display: inline-flex;
        width: 100%;
        align-items: center;
        font-size: 0.9rem;
      }

      .asset-input input[type="file"] {
        width: 100%;
        border: 1px dashed rgba(17, 17, 17, 0.3);
        border-radius: 0.75rem;
        padding: 0.65rem 0.75rem;
        font-size: 0.9rem;
        background-color: rgba(14, 165, 233, 0.06);
      }

      .form-actions {
        display: flex;
        justify-content: flex-end;
      }

      .form-actions__submit {
        border: none;
        border-radius: 999px;
        padding: 0.85rem 1.75rem;
        font-size: 0.95rem;
        font-weight: 600;
        letter-spacing: 0.02em;
        text-transform: uppercase;
        background: linear-gradient(135deg, #0ea5e9, #0284c7);
        color: #fff;
        cursor: pointer;
        box-shadow: 0 12px 24px rgba(2, 132, 199, 0.25);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }

      .form-actions__submit:hover {
        transform: translateY(-1px);
        box-shadow: 0 16px 28px rgba(2, 132, 199, 0.28);
      }

      .form-actions__submit:focus {
        outline: none;
        box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.3);
      }
    </style>
  </head>
  <body>
    <main class="wrapper">
      <section class="intro">
        <h1 class="intro__title">New Website Client Intake</h1>
        <p class="intro__lead">
          This interview captures the language, proof, and visuals we need to draft your full site copy. Take your time—rich answers today mean a site that already sounds like you tomorrow.
        </p>
        <ol class="intro__list">
          <li>
            <span class="intro__list-number">1</span>
            <span>Map your story, positioning, and social proof.</span>
          </li>
          <li>
            <span class="intro__list-number">2</span>
            <span>Outline services, offers, and FAQs so we can shape every page.</span>
          </li>
          <li>
            <span class="intro__list-number">3</span>
            <span>Drop visuals or inspiration to guide the mood and references.</span>
          </li>
        </ol>
      </section>

      <form class="intake-form" method="post" action="/api/client-onboarding" enctype="multipart/form-data">
        <fieldset class="form-section">
          <legend class="form-section__legend">Project Snapshot</legend>
          <div class="form-grid form-grid--two">
            <label class="form-field">
              <span class="form-field__label">Business name *</span>
              <input class="form-control" name="businessName" required placeholder="Tender Gardens Co." />
            </label>
            <label class="form-field">
              <span class="form-field__label">Primary contact *</span>
              <input class="form-control" name="primaryContact" required placeholder="Name we’ll partner with" />
            </label>
            <label class="form-field">
              <span class="form-field__label">Email *</span>
              <input class="form-control" type="email" name="email" required placeholder="hello@example.com" />
            </label>
            <label class="form-field">
              <span class="form-field__label">Phone</span>
              <input class="form-control" name="phone" placeholder="(555) 123-4567" />
            </label>
          </div>
          <div class="form-grid form-grid--two">
            <label class="form-field">
              <span class="form-field__label">Existing site / important links</span>
              <textarea class="form-control" data-rows="short" name="keyLinks" placeholder="Current site, socials, booking links, brochures, etc."></textarea>
            </label>
            <label class="form-field">
              <span class="form-field__label">Timeline or launch goal</span>
              <input class="form-control" name="timeline" placeholder="Target launch date, milestones, promos" />
            </label>
          </div>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Brand Story &amp; Voice</legend>
          <label class="form-field">
            <span class="form-field__label">Tagline or signature phrase</span>
            <input class="form-control" name="tagline" placeholder="Joyful outdoor spaces for modern homes" />
          </label>
          <label class="form-field">
            <span class="form-field__label">Elevator pitch (2–3 sentences)</span>
            <textarea class="form-control" data-rows="short" name="elevatorPitch" placeholder="How do you introduce the business when someone asks what you do?"></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Brand voice adjectives</span>
            <input class="form-control" name="brandAdjectives" placeholder="Warm, playful, editorial, luxe, welcoming..." />
          </label>
          <label class="form-field">
            <span class="form-field__label">Origin story / why you started</span>
            <textarea class="form-control" name="brandStory" placeholder="Share the backstory, pivotal moments, or beliefs that drive the brand."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Differentiators</span>
            <textarea class="form-control" data-rows="short" name="differentiators" placeholder="What makes you the obvious choice over competitors?"></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Audience &amp; Outcomes</legend>
          <label class="form-field">
            <span class="form-field__label">Primary audience segments</span>
            <textarea class="form-control" name="audiences" placeholder="Describe each key client type, their world, and what they value."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Top problems you solve</span>
            <textarea class="form-control" data-rows="short" name="problemStatements" placeholder="Think in their words—what frustrations or gaps do they bring to you?"></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Transformations or outcomes clients rave about</span>
            <textarea class="form-control" data-rows="short" name="transformations" placeholder="What changes after someone works with you? How do they feel?"></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Primary calls-to-action</span>
            <textarea class="form-control" data-rows="short" name="primaryCtas" placeholder="Book a consult, request a design plan, join waitlist, include any links."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Hero &amp; Key Messages</legend>
          <label class="form-field">
            <span class="form-field__label">Dream homepage headline</span>
            <textarea class="form-control" data-rows="short" name="heroHeadline" placeholder="If visitors remember one sentence, make it this one."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Supporting sub-headline</span>
            <textarea class="form-control" data-rows="short" name="heroSubheadline" placeholder="Clarify what you do, for whom, and the result."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Homepage highlight blurbs</span>
            <textarea class="form-control" name="homepageHighlights" placeholder="List 3–4 quick-hit benefits, stats, or offerings with short descriptions."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Services &amp; Offers</legend>
          <p class="form-hint">Describe each offer: who it’s for, what’s included, how it works, pricing ranges, timelines, and wins.</p>
          <label class="form-field">
            <span class="form-field__label">Signature offer</span>
            <textarea class="form-control" name="serviceSignature" placeholder="Name, promise, key deliverables, investment range, timeline."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Supporting offer #1</span>
            <textarea class="form-control" name="serviceOne" placeholder="Name, promise, key deliverables, investment range, timeline."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Supporting offer #2</span>
            <textarea class="form-control" name="serviceTwo" placeholder="Name, promise, key deliverables, investment range, timeline."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Add-on services or seasonal offers</span>
            <textarea class="form-control" data-rows="short" name="serviceAddons" placeholder="List any a la carte services, retainers, or recurring items."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Proof &amp; Social Credibility</legend>
          <label class="form-field">
            <span class="form-field__label">Testimonials (copy/paste or paraphrase)</span>
            <textarea class="form-control" name="testimonials" placeholder="Include client name / role and any context. Share as many as you have."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Stats, awards, media, or certifications</span>
            <textarea class="form-control" data-rows="short" name="proofPoints" placeholder="E.g. 150+ gardens maintained, featured in XYZ, Certified Arborist."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Portfolio notes</span>
            <textarea class="form-control" data-rows="short" name="portfolioNotes" placeholder="Highlight projects to showcase or stories to tell around them."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Process, FAQs &amp; Experience</legend>
          <label class="form-field">
            <span class="form-field__label">Your process (phases or steps)</span>
            <textarea class="form-control" name="process" placeholder="Break down how you guide clients from hello to handoff. Include timelines if possible."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Top FAQs &amp; how you answer them</span>
            <textarea class="form-control" name="faqs" placeholder="Think about pricing, timeline, availability, deliverables, expectations."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Client experience touches</span>
            <textarea class="form-control" data-rows="short" name="clientExperience" placeholder="Welcome gifts, check-ins, tools, or moments that make working with you special."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Visual Direction</legend>
          <label class="form-field">
            <span class="form-field__label">Color palette / mood</span>
            <input class="form-control" name="visualPalette" placeholder="Hex codes, vibes, seasonal inspiration, etc." />
          </label>
          <label class="form-field">
            <span class="form-field__label">Visual references</span>
            <textarea class="form-control" data-rows="short" name="visualReferences" placeholder="Drop URLs, Pinterest boards, or describe the atmosphere we should capture."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Anything to avoid visually</span>
            <textarea class="form-control" data-rows="short" name="visualAvoid" placeholder="Colors, fonts, imagery, or words you never want to see."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Logistics &amp; Contact Details</legend>
          <div class="form-grid form-grid--two">
            <label class="form-field">
              <span class="form-field__label">Service areas or regions</span>
              <input class="form-control" name="serviceAreas" placeholder="Neighborhoods, cities, or remote availability" />
            </label>
            <label class="form-field">
              <span class="form-field__label">Operating hours / booking cadence</span>
              <input class="form-control" name="operatingHours" placeholder="M–F 9-5, consults on Thursdays, seasonal pauses" />
            </label>
          </div>
          <label class="form-field">
            <span class="form-field__label">Contact form fields you need</span>
            <textarea class="form-control" data-rows="short" name="contactRequirements" placeholder="Name, email, project budget, location, service interest, etc."></textarea>
          </label>
          <label class="form-field">
            <span class="form-field__label">Legal notes or disclaimers</span>
            <textarea class="form-control" data-rows="short" name="legalNotes" placeholder="Contracts, licensing info, liability statements, or policy reminders."></textarea>
          </label>
        </fieldset>

        <fieldset class="form-section">
          <legend class="form-section__legend">Visual Assets</legend>
          <p class="form-hint">Upload up to five photos, mood shots, or logos (max 5&nbsp;MB each). Files land inside the repo for easy reference.</p>
          <label class="form-field asset-input">
            <input type="file" name="assets" accept="image/*" multiple />
          </label>
        </fieldset>

        <label class="form-field">
          <span class="form-field__label">Anything else we should know?</span>
          <textarea class="form-control" name="notes" placeholder="Workflow preferences, past site pain points, future ideas—drop them here."></textarea>
        </label>

        <div class="form-actions">
          <button class="form-actions__submit" type="submit">Send Intake</button>
        </div>
      </form>
    </main>
  </body>
</html>
