# GitHub Integration (Required for production form submissions)
# These variables enable the client onboarding form to commit submissions
# to a separate 'submissions' branch in your GitHub repository.

# Your GitHub personal access token with 'repo' scope
# Create one at: https://github.com/settings/tokens/new
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_your_token_here

# Your GitHub username or organization name
# Example: if your repo is github.com/johndoe/tender-gardens, use "johndoe"
GITHUB_OWNER=desland01

# Your repository name
# Example: if your repo is github.com/johndoe/tender-gardens, use "tender-gardens"
GITHUB_REPO=Tender-gardens

# Email notifications (Optional but recommended)
# Create an API key at https://resend.com/ and drop it in below.
# RESEND_TO defaults to desmond@grovestreetpainting.com if omitted.
# RESEND_FROM defaults to Tender Gardens Intake <intake@resend.dev> (works without domain verification).
# Uncomment to use a custom sender only after verifying it with Resend.
RESEND_API_KEY=your-resend-api-key
# RESEND_TO=desmond@grovestreetpainting.com
# RESEND_FROM=Tender Gardens Intake <intake@resend.dev>

# Analytics (Optional)
# Uncomment and add your tracking IDs if you want to enable analytics
# PUBLIC_GA_ID=G-XXXXXXXXXX
# PUBLIC_GTM_ID=GTM-XXXXXXX
